export const settings = {
    height: 'auto',          // slides heigth of carousel component
    slidesToShow: document.documentElement.clientWidth > 720 ? 2 : 1,        // slides on screen at same time
    spaceBetweenSlides: 16, // in px
    showDots: true,         // true/false
    showAuto: true,         // true/false
    autoInterval: 2000,     // ms
};